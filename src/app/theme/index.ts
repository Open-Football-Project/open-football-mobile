import {
  colors
} from './colors';

import {
  spacing,
  borderRadius,
  borders,
  shadows,
  sizes,
  zIndex,
  lineHeights,
  opacity,
} from './layout';

import {
  fontFamily,
  fontSize,
  fontWeight,
  textStyles,
  letterSpacing,
} from './typography';

export { colors } from './colors';

export {
  spacing,
  borderRadius,
  borders,
  shadows,
  sizes,
  zIndex,
  lineHeights,
  opacity,
  breakpoints,
} from './layout';

export {
  fontFamily,
  fontSize,
  fontWeight,
  textStyles,
  letterSpacing,
} from './typography';

export const theme = {
  colors,
  spacing,
  borderRadius,
  borders,
  shadows,
  sizes,
  zIndex,
  lineHeights,
  opacity,
  fontSize,
  fontWeight,
  fontFamily,
  textStyles,
  letterSpacing,
};
